let names = ['vaxarshak', 'petros', 'poxos', 'martiros', 'esimov'];
for(let k in names){
  console.log(`${k} for in`);//log(the keys of array);
}

for(let d of names){
  console.log(`${d} for of`);
}
